rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules are for debugging only and make your database public.
      // Anyone can read, write, and delete data.
      allow read, write: if true;
    }
  }
}
